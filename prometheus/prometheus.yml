global:
  scrape_interval: 30s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts/*.rules.yml

scrape_configs:
  - job_name: keycensus
    scrape_interval: 60s
    static_configs:
      - targets: ["keycensus:9742"]
